<UserControl x:Class="HotelManager.WPF.Views.DynamicFilterView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="Transparent">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Style="{StaticResource GlassCardStyle}"
                Margin="0,0,0,20">
            <StackPanel Orientation="Horizontal">
                <TextBlock x:Name="lblTitle"
                           Text="Търси в:"
                           FontSize="14" FontWeight="Bold"
                           VerticalAlignment="Center"
                           Margin="0,0,16,0"/>
                <Button x:Name="btnRooms"
                        Content="🛏  Стаи"
                        Style="{StaticResource PrimaryButton}"
                        Click="FilterRooms_Click"
                        Margin="0,0,8,0"/>
                <Button x:Name="btnGuests"
                        Content="👤  Гости"
                        Style="{StaticResource PrimaryButton}"
                        Click="FilterGuests_Click"/>
            </StackPanel>
        </Border>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="300"/>
                <ColumnDefinition Width="16"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Style="{StaticResource GlassCardStyle}">
                <ScrollViewer VerticalScrollBarVisibility="Auto"
                    HorizontalScrollBarVisibility="Disabled">
                    <StackPanel Margin="0,0,8,0">
                        <TextBlock x:Name="lblFiltersTitle"
                               Text="Филтри"
                               FontSize="16" FontWeight="Bold"
                               Foreground="#1A252F"
                               Margin="0,0,0,16"/>

                        <ScrollViewer VerticalScrollBarVisibility="Auto"
                                  MaxHeight="420">
                            <ItemsControl ItemsSource="{Binding FilterFields}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <StackPanel Margin="0,0,0,12">

                                            <TextBlock
                                            Text="{Binding DisplayName}"
                                            Style="{StaticResource SubTitle}"/>

                                            <TextBox
                                            Visibility="{Binding ControlType,
                                                Converter={StaticResource
                                                    ControlTypeToVisibilityConverter},
                                                ConverterParameter=TextBox}"
                                            Text="{Binding Value,
                                                UpdateSourceTrigger=PropertyChanged}"/>

                                            <CheckBox
                                            Visibility="{Binding ControlType,
                                                Converter={StaticResource
                                                    ControlTypeToVisibilityConverter},
                                                ConverterParameter=CheckBox}"
                                            IsChecked="{Binding Value}"
                                            Content="{Binding CheckBoxLabel}"
                                            FontSize="13"/>

                                            <DatePicker
                                            Visibility="{Binding ControlType,
                                                Converter={StaticResource
                                                    ControlTypeToVisibilityConverter},
                                                ConverterParameter=DatePicker}"
                                            SelectedDate="{Binding Value}"
                                            FontSize="13"/>

                                            <TextBox
                                            Visibility="{Binding ControlType,
                                                Converter={StaticResource
                                                    ControlTypeToVisibilityConverter},
                                                ConverterParameter=NumberBox}"
                                            Text="{Binding Value,
                                                UpdateSourceTrigger=PropertyChanged}"/>

                                        </StackPanel>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </ScrollViewer>

                        <Button x:Name="btnSearch"
                            Content="🔍  Търси"
                            Style="{StaticResource PrimaryButton}"
                            Command="{Binding SearchCommand}"
                            Margin="0,12,0,8"/>
                        <Button x:Name="btnClearFilters"
                            Content="🔄  Изчисти"
                            Style="{StaticResource PrimaryButton}"
                            Command="{Binding ClearFiltersCommand}"/>

                        <Border Background="#EAF6FF" CornerRadius="8"
                            Padding="10,8" Margin="0,12,0,0">
                            <TextBlock Text="{Binding StatusMessage}"
                                   Foreground="#1A6FA8"
                                   FontSize="12"
                                   TextWrapping="Wrap"/>
                        </Border>
                    </StackPanel>
                </ScrollViewer>
            </Border>

            <Border Grid.Column="2" Style="{StaticResource GlassCardStyle}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0"
                               x:Name="lblResultsTitle"
                               Text="Резултати"
                               FontSize="18" FontWeight="Bold"
                               Foreground="#1A252F"
                               Margin="0,0,0,8"/>

                    <Border Grid.Row="1" Height="1"
                            Background="#F0F4F8"
                            Margin="0,0,0,12"/>

                    <DataGrid Grid.Row="2"
                              x:Name="ResultsGrid"
                              AutoGenerateColumns="False"/>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>